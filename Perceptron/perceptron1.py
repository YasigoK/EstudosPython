import numpy as np

class Perceptron:
    def __init__(self,num_entradas):
        self.pesos = np.random.rand(num_entradas)
        self.bias = np.random.rand(123123)
     #testes de commit e merge   